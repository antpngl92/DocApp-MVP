const HOME_PAGE_SERVICE_KEYS = ["online", "deposit", "privacy", "details"] as const;

const HOME_PAGE_STEP_KEYS = ["service", "time", "deposit"] as const;

const HOME_PAGE_POLICY_KEYS = ["deposit", "attendance", "privacy"] as const;

const HOME_PAGE_CONTACT_KEYS = ["online", "deposit", "balance"] as const;

export {
  HOME_PAGE_CONTACT_KEYS,
  HOME_PAGE_POLICY_KEYS,
  HOME_PAGE_SERVICE_KEYS,
  HOME_PAGE_STEP_KEYS,
};
