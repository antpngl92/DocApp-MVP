type AccountSignInProps = Readonly<{
  clerkSignIn: React.ReactNode;
  description: string;
  eyebrow: string;
  helpText: string;
  securityNote: string;
  title: string;
}>;

export type { AccountSignInProps };
