import { CheckoutStatusView } from "@/features/app-foundation/components";

const CheckoutExpiredPage = () => {
  return <CheckoutStatusView status="expired" />;
};

export default CheckoutExpiredPage;
