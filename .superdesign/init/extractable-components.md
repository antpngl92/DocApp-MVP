# Extractable Components

## Summary

No existing shared layout or UI components are ready for SuperDesign component extraction yet.

This is expected because the MVP rebuild intentionally did not port prototype Radix/shadcn components or old reusable UI components.

## Layout Components

None currently implemented.

Expected future components after approved design:

- `AppShell`
- `AppHeader`
- `AdminShell`
- `PatientShell`
- `PublicBookingShell`

## Basic Components

None currently implemented.

Expected future components after approved design:

- `Button`
- `Panel`
- `StatusBadge`
- `EmptyState`
- `LoadingState`
- `PriceSummary`
- `SlotStateBadge`
- `AppointmentStatusBadge`
- `PaymentStatusBadge`
- `CalendarSyncStatusBadge`

## Extraction Guidance

Do not extract old prototype UI components.

Create new components only after a reviewed SuperDesign direction exists and Phase 4+ implementation begins.
